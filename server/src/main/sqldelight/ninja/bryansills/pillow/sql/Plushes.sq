CREATE TABLE IF NOT EXISTS plush (
  _id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  coolness_level INT NOT NULL
);

selectAll:
SELECT *
FROM plush;

insertPlush:
INSERT INTO plush (name, coolness_level)
VALUES (?, ?);